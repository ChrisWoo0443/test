# Optional — the in-app setup wizard is the normal way to configure
# credentials. Set these only if you want to skip the wizard; env vars
# take priority over spotify-credentials.json.
SPOTIFY_CLIENT_ID=your_spotify_client_id_here
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret_here
SPOTIFY_REDIRECT_URI=http://localhost:3001/auth/callback
